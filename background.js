/**
 * background.js — Loki Service Worker
 *
 * This background script is currently kept as a placeholder.
 * Chrome natively maps the `_execute_action` command to open the extension popup
 * and all navigation / storage actions are executed directly inside the popup.
 */
